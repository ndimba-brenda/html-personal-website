# html-personal-website
This is a simple personal website is created using HTML only. It demonstrated my understanding of some HTML elements and attributes by creating a personal portfolio.
Personal Website Using HTML

Project Description

This project is a personal website created using HTML only. The website demonstrates my understanding of HTML elements, attributes, semantic structure, navigation, tables, forms, images, lists, and organized content sections.

Features

The website contains:

- Home section
- About Me section
- Education section
- Skills section
- Hobbies and Interests section
- Future Goals section
- Contact section
- Navigation links
- Educational background table
- Contact form
- Profile image

Question 1: Website Creation

The website was created using only HTML and contains more than 25 different HTML elements and more than 15 different HTML attributes. Semantic elements were used to organize the content into clear sections.

Question 2: HTML Elements

##1. Five challenging HTML elements

The five elements I found most challenging were:

1. "<table>" – It was challenging because I had to understand how "<table>", "<tr>", "<th>", and "<td>" work together to organize information.

2. "<form>" – The form required several elements such as "<label>", "<input>", "<textarea>", and "<button>" to work together correctly.

3. "<details>" – This element was challenging because it is less familiar than common HTML elements and creates expandable content.

4. "<nav>" – I had to understand how navigation links work with the "href" and "id" attributes.

5. "<img>" – I had to correctly use the "src" and "alt" attributes and make sure the image file was in the correct location.

##2. Use of semantic elements

I used semantic HTML elements to organize my website. The "<header>" contains the website title, introduction, and navigation menu. The "<main>" contains the main content, while "<section>" elements separate areas such as About Me, Education, Skills, Hobbies, Goals, and Contact.

The "<article>" element was used for independent pieces of content, while the "<footer>" contains copyright information and the Back to Top link.

Using semantic elements makes the website easier to understand, maintain, and navigate.

##3. Most useful element for organizing the layout

The "<section>" element was the most useful because it allowed me to divide the webpage into clear and meaningful parts. Each section has a specific purpose, making the website more organized and easier to navigate.

Question 3: HTML Attributes

##1. Three essential attributes

The three attributes I found most essential were:

- "href" – Used to create navigation links between different sections of the webpage.
- "src" – Used to specify the location of my profile image.
- "id" – Used to give sections unique identifiers and connect them to navigation links.

##2. Difference between class and id

The "id" attribute identifies a specific element with a unique name. I used IDs such as "about", "education", and "contact" for different sections of my website.

The "class" attribute is normally used when several elements need to share the same classification or styling. Since my website was created using HTML only and did not require CSS, I mainly used the "id" attribute.

##3. Attribute that improved user experience

The "href" attribute improved the user experience the most because it allowed me to create navigation links. Users can click a link and move directly to a particular section of the webpage instead of scrolling through the entire page.

Question 4: Development Process

##1. Planning the website structure

Before coding, I identified the information I wanted to include and divided it into sections:

- Home
- About Me
- Education
- Skills
- Hobbies and Interests
- Future Goals
- Contact

I then selected appropriate semantic HTML elements for each section and planned the navigation links.

##2. Testing and debugging

I tested the website by opening the HTML file in a web browser and checking whether the content displayed correctly.

I checked the navigation links, image, table, and contact form. When I found errors, I checked for missing closing tags, incorrect attributes, spelling mistakes, broken links, and incorrect file paths.

After making corrections, I refreshed the webpage to confirm that the errors had been fixed.

##3. Challenges faced

One challenge was understanding how different HTML elements work together, especially the table and contact form. I overcame this by learning the purpose of each element and building the sections step by step.

Another challenge was creating navigation between different sections. I solved this by using unique "id" values together with the "href" attribute.

I also had difficulty displaying the profile image. I solved this by placing the image in the correct folder and using the correct filename in the "src" attribute.

Question 5: Git & GitHub Implementation

##1. Git commands used

The Git commands I used included:

git init
git status
git add .
git commit -m "Initial website structure"
git branch -M main
git remote add origin
git push -u origin main

These commands helped me initialize my repository, track changes, save versions of my work, connect my project to GitHub, and upload my files.

##2. Number of commits and commit strategy

I made [5] commits during development.

I used short and descriptive commit messages to explain the changes made. Examples include:

- "Initial website structure"
- "Added About Me section"
- "Added education and skills sections"
- "Added contact form"
- "Updated navigation links"
- "Fixed HTML errors"

This made it easier to track the development of my project.

3. Importance of version control

Version control is important because it keeps a record of changes made to a project. It allows developers to track their progress, identify mistakes, and return to previous versions when necessary.

It also makes collaboration easier and allows projects to be stored and shared through platforms such as GitHub.

Question 6: Code Quality & Best Practices

1. Ensuring valid and error-free HTML

I checked my HTML code to make sure that elements were correctly opened and closed and that attributes were properly written.

I tested the webpage in a browser and checked the image, navigation links, table, and form. I also checked for missing tags, spelling mistakes, incorrect attributes, and incorrect file paths.

2. Best practices followed

I followed several best practices, including:

- Using proper indentation.
- Using semantic HTML elements.
- Using meaningful IDs.
- Adding comments to major sections.
- Using lowercase HTML elements and attributes consistently.
- Providing "alt" text for images.
- Keeping related content together.
- Organizing the code into clear sections.

These practices made the code easier to read, understand, and maintain.

3. Future improvements

If I had more time, I would improve the website by adding CSS to create a more professional design, including colors, fonts, spacing, responsive layouts, and improved navigation.

I would also add JavaScript features such as form validation, dark mode, and interactive buttons.

Other improvements would include:

- Adding a portfolio section.
- Adding social media links.
- Improving the contact form.
- Making the website responsive for mobile devices.
- Adding CSS animations.
- Improving accessibility.
- Hosting the website using GitHub Pages.

Technologies Used

- HTML5
- Git
- GitHub

Author

Brenda

License

This project was created for educational purposes.
